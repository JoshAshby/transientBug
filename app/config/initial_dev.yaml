users:
  - username: bug
    password: josh
    groups:
      - admin
      - root

tables:
  - users
  - notes
  - phots

buckets:
  enable500Error:
    value: true
    name: Enable errors on 500 pages
    description: Enable the display of the server errors on 500 pages

reset_users: false
flush_redis: true
